# Guild AI MNIST models

This package provides a softmax and CNN classifier for MNIST hand
written digits. It's primary used for getting started with Guild AI
and for testing.

Refer to [`guild.yml`](guild.yml) for details.
