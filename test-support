#-*-shell-script-*-

set -e

_assert_root() {
    if [ -z "$root" ]; then
        echo "root is not defined, quitting"
        return 1
    fi
}

init-test-env() {
    _assert_root
    test -e test/env || PYTHONPATH=$root guild init -y -p $root test/env
    QUIET=1 source guild-env test/env
}
